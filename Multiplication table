#include <stdio.h>
int main()
{
  int n,m;
  scanf("%d %d",&n,&m);
  printf("Enter n\nEnter m\nThe multiplication table of %d is\n",n);
  for(int i = 1; i<=m; i++)
  {
    printf("%d*%d=%d\n",i,n,(i*n));
  }
  return 0;
}
